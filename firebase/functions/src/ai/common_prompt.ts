export const EXTRA_INSTRUCTION = `
You must respond with ONLY a pure JSON object.
DO NOT include \`\`\`json, markdown formatting, comments, or any explanation.
The response MUST start with { and end with }.
`;

